<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ImageView
        android:id="@+id/RptToPolice"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/Emergency"
        android:layout_marginRight="14dp"
        android:contentDescription="@null"
        android:src="@mipmap/report_to_police" />

    <ImageView
        android:id="@+id/Connect"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_alignRight="@+id/Emergency"
        android:layout_below="@+id/Emergency"
        android:layout_marginTop="15dp"
        android:contentDescription="@null"
        android:src="@mipmap/connect" />

    <ImageView
        android:id="@+id/CheckPoint"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_alignLeft="@+id/RptToPolice"
        android:layout_alignTop="@+id/Connect"
        android:contentDescription="@null"
        android:src="@mipmap/checkpoint" />

    <ImageView
        android:id="@+id/Emergency"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_alignParentTop="true"
        android:layout_marginRight="21dp"
        android:layout_marginTop="87dp"
        android:layout_toLeftOf="@+id/RptToPolice"
        android:contentDescription="@null"
        android:src="@mipmap/emergency" />

</RelativeLayout>
